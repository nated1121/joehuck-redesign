# Joe Huck Electric — homepage redesign

Single-file homepage (`index.html`). Open it in a browser; no build step.

- Brand kept: khaki-bronze header, brown buttons, Barlow type, with white, silver and black.
- Before launch: replace the recreated wordmark with the official logo file, and connect the estimate form to a form handler (see the TODO in the script).
